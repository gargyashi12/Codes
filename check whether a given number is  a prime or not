#include <stdio.h>
int main()
{
	int n,i,j,c=0;
	scanf("%d",&n);
	for(i=2;i*i<=n;i++)
	{
		if(n%i==0)
		c++;
	}
	if(c!=0 || n==1)
	printf("%d is not a prime number",n);
	else
	printf("%d is a prime number",n);
}
